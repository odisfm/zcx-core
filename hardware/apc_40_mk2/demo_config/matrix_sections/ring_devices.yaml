pad_group: ring_devices
type: param
on_color: purple
off_color: purple shade 1
binding:
  default: >
    RING(${me.x}) / DEV(1) B1 P${me.global_Y}
  __shift: >
    RING(${me.x}) / DEV(1) B1 P${me.global_Y + me.section.height}
gestures:
  long_release:
    do_toggle: true
