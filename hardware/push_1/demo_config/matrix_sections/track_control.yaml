type: ring_track
ring_index: ${me.index + ring.width}
gestures:
  press: >
    "${me.track}" / SEL
  long_press: >
    ALL / ARM OFF ;
    "${me.track}" / ARM
  long_press__shift: >
    "${me.track}" / ARM
  double_click: >
    "${me.track}" / PLAY >
  press__delete: >
    "${me.track}" / DEL
